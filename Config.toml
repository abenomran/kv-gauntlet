system = "etcd"
workload = "balanced"
duration_seconds = 60
concurrency = 4
output_file = "results.csv"

# Optional: remove this section to run without fault injection
[fault]
script = "scripts/kill_node2.sh"
trigger_at_seconds = 30